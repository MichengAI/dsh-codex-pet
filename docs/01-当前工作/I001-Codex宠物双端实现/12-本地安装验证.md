# 本地安装验证

更新时间：2026-09-09 13:40（Asia/Shanghai）

用户要求安装到本地进行测试，已执行。

- 目标：`C:\Users\YUJIYU\.dsh\profiles\web`。
- 包：`@michengai/dsh-codex-pet` 0.1.0，本地 link 指向当前工作区，已加入 bundles。
- 配置备份：`C:\Users\YUJIYU\.dsh\local-backups\codex-pet-20260909-133707`。
- 安装复用 Desktop 的 `D:\Tools\DSH Codex Desktop\plugins\store`。DSH CLI 对带空格的参数转发有问题，因此使用 pnpm 直接安装，再通过 `dsh plugin --profile web list --depth 0` 完成 bundle 列表同步。
- 原有依赖版本与来源比较：0 项改变。
- 已启动本机 `D:\Tools\DSH Codex Desktop\DSH Codex Desktop.exe`。
- 实际服务 API 返回：9 只宠物、Codex 已选、显示开启、0 项资源警告。
- 当前安装的 Desktop 为 1.0.50，安装包中未发现 `pet-window.js`，因此当前安装版测试页面浮层；原生桌面窗口仍需包含桥接的 Desktop 版本。
- 创建维持 Skill 指令交接流程，未启用或声称自动生图。

入口：Desktop → 设置 → 宠物。卸载只删除本插件依赖及 bundle 项，保留用户宠物数据；若之后已有其他配置变更，不用整份旧备份覆盖最新配置。
