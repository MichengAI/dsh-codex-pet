# I001 — Codex 宠物双端实现

- 状态：进行中
- 负责人：Codex；后续验收负责人待定
- 优先级：P1
- 更新时间：2026-09-09 15:04（Asia/Shanghai）
- 关联提交：插件核心 `d6a46f0`、测试 `f3a8b4e`；仓库文档随本记录提交，见 Git 历史。相邻 Desktop 收尾时已出现 `ccf5d99 feat: refresh bundled plugins and add desktop pet support`，不是本任务执行的提交。

## 目标与边界

实现原版内置库、Skill 创建入口、Web 页内和 Desktop 桌面显示；参考四仓但不复制实现。当前交付本地开发版，不发布、不替换正常 Desktop 安装包。

## 任务与验收

| 任务 | 结果及证据 |
|---|---|
| 原版 9 宠导入 | 已实现；原始 WebP、来源索引和 SHA256，9 宠几何检查通过 |
| 设置与网页交互 | 已实现；选宠刷新持久、收起恢复、对话框、窄屏已验证 |
| DSH 插槽 | 基础 DSH 和实际 dsh-codex-ui 组合均已加载；见 08 截图 |
| Desktop 原生窗口 | 已实现；独立、透明、置顶、状态同步、最大尺寸边界、恶意资源拒绝和关闭测试通过 |
| 创建 Skill | DSH 新会话与随包 Skill 已接入；完整生图未验收，见 14-独立运行改造.md |
| 真实任务联动 | 多会话与直接交互双端受控验收通过；真实模型链路未验收，见 [16-多会话验收](16-多会话验收.md) |
| 视觉 QA | 见 [11-设计验收](11-设计验收.md) |

## 验证记录

- 用户授权的正常 profile 安装已完成，见 [12-本地安装验证](12-本地安装验证.md)。

- 插件 `npm run check`、`npm test`（16/16）、`npm run build` 通过。
- Desktop `npm run build`、`npx tsx --test test\pet-window-policy.test.ts`（2/2）通过。
- `scripts/smoke-desktop.cjs` 在真实 Electron 44.1.1 隔离实例执行通过，包括最大 224 尺寸宠物和气泡仍在窗口内。
- npm 官方 registry 的依赖审计在本轮返回 0 已知漏洞；不承诺未来无漏洞。
- 浏览器复制创建指令后显示真实成功状态；未发送生成请求，没有创建测试宠物到用户目录。
- CLI 只读能力核对：无图像生成工具、无 `load_workspace_dependencies`。WindowsApps 原始 exe 启动被拒绝，PATH 中 Codex CLI 可执行。
- 浏览器最终预览错误/警告日志检查为空。完整模型调用、外部发布与安装包替换未执行。

## 跨仓库改动与恢复

Desktop 新增 `src/pet-window.ts`、`src/pet-window-policy.ts`、`src/pet-window-preload.cts`、`test/pet-window-policy.test.ts`；修改 `src/main.ts` 安装/清理桥接和 `src/dsh-view-preload.cts` 暴露有限 API。

Desktop 的 CHANGELOG、README、bundled-plugins 及相关测试在本轮前已有未提交改动，不属于本插件交付，不覆盖或回退。Codex UI 未作源码修改。

收尾只读复核发现 Desktop 工作区已干净，宠物文件被上述新提交纳入；保留该外部变化，不改写历史。恢复策略应以实际最新提交为准，只对宠物范围制作新的逆向修改。

恢复时卸载本插件并重启；Desktop 只移除上述宠物桥接变更。保留用户 `.codex\pets`、已有配置和其他未提交内容。历史隔离测试数据位于 `.preview`，清理被工具策略阻止，未进入版本控制。

## 阻塞与待处理

统一维护在 [待办与阻塞](../../00-交接入口/03-待办与阻塞.md)。这些事项未完成前不将本迭代标成已完成或归档。

## 阅读顺序与证据

先读 [产品定义](../../02-产品与业务/00-宠物产品定义.md)、[接口与架构](../../03-技术架构/00-宠物接口与架构.md)，再读 [设计验收](11-设计验收.md)。

- [01 原图](01-Codex参考.png)、[03 初版](03-设置初版.png)、[04 修正版](04-设置修正版.png)
- [05 全图对照](05-原图与实现对照.jpg)、[07 局部对照](07-设置局部对照.png)
- [06 原生桌面窗口](06-桌面窗口.png)、[08 Codex UI 浅色集成](08-CodexUI浅色集成.jpg)
- [09 窄屏](09-窄屏设置.png)、[10 Skill 交接](10-Skill创建交接.png)


- 最新独立运行改造及替代旧 Codex 交接方案见 [14-独立运行改造](14-独立运行改造.md)。上文 CLI 探测记录为历史证据，不代表当前运行依赖。

- 多会话功能开发与双端受控验收已完成，见 [16-多会话验收](16-多会话验收.md)。迭代因真实生成及正式 Desktop 安装验收仍保持进行中。

- 按用户反馈移除通知面板的新会话下拉、消息输入和发送入口，保留通知及待处理操作；类型检查、实际客户端渲染和 Desktop 策略测试通过。最新截图 [17-精简会话通知](17-精简会话通知.png)。

- GitHub 私有仓库、CodeGraph 和预览移除记录见 [24-仓库初始化与清理](24-仓库初始化与清理.md)。本次未发布 Release 或 npm 包。
