# 仓库初始化与清理

更新时间：2026-09-09 15:04（Asia/Shanghai）

## 已完成

- 初始化 main 分支，创建私有仓库 https://github.com/MichengAI/dsh-codex-pet 。
- 初始化并同步 CodeGraph，本地索引通过状态检查；`.codegraph/` 不提交。
- 删除 `scripts/preview.ts` 与开发预览命令，`/preview.html` 返回 404；保留桌面浮窗必需的 `/desktop.html` 和构建入口。
- 插件设置回退使用真实设置对话框，移除预览页面依赖。
- 忽略测试数据、日志、索引、构建产物、依赖和旧安装包；保留九只图集及历史验收图片。
- 分批提交：核心 `d6a46f0`、测试 `f3a8b4e`，文档提交见包含本文件的 Git 历史。

## 验证

- 类型检查、16 项测试、构建通过。
- 真实 Electron 隔离客户端 smoke 通过，包含设置对话框、多会话和空状态。
- HTTP 回归确认预览 404、桌面入口 200。
- 待提交文本常见令牌与私钥模式扫描未命中；不代表全面安全审计。

## 未完成与保留范围

`.preview`（含 493 个 Junction）和旧 `michengai-dsh-codex-pet-0.1.0.tgz` 的删除被自动审批审查拒绝，仅返回 `blocked by policy`，因此仍留在本机、未提交。待允许清理的环境处理；删除 Junction 时只移除链接本身。

`lib`、`node_modules` 是本地链接安装运行所需，保留。`assets/codex/source.json` 是本机来源记录，保留且不提交。不改用户 DSH/Codex 数据或相邻 Desktop 仓库。

I001 保持进行中，真实生图、模型联动和正式 Desktop 安装验收仍见[待办](../../00-交接入口/03-待办与阻塞.md)。没有发布 Release 或 npm 包。
