# 设计验收

更新时间：2026-09-09 12:40（Asia/Shanghai）

按 Product Design 的 design-qa 流程执行；用户项目文档编号与位置约定优先，因此本报告位于本迭代目录，不在项目根目录另建平行报告。

## 视觉真值与比较条件

- 原图：[01-Codex参考.png](01-Codex参考.png)，2272×1359 像素。
- 实现：[04-设置修正版.png](04-设置修正版.png)，2272×1359 像素。
- CSS viewport：2272×1359，deviceScaleFactor 1；两个图像以相同像素尺寸直接拼接，没有把放大截图当作 1:1 比较。
- 状态：深色、选 Codex、9 只宠物、大小 120。原图有真实任务气泡，预览没有模型任务，因此不伪造同一气泡；气泡另用原生窗口受控状态验收。
- 完整组合：[05-原图与实现对照.jpg](05-原图与实现对照.jpg)，已打开检查整体布局。
- 局部组合：[07-设置局部对照.png](07-设置局部对照.png)，左右分别为原图和实现，已打开检查列表、文案、控件、间距和图像。
- 补充状态：[08-CodexUI浅色集成.jpg](08-CodexUI浅色集成.jpg)、[09-窄屏设置.png](09-窄屏设置.png)、[10-Skill创建交接.png](10-Skill创建交接.png)。窄屏实测 CSS 390×844、DPR 1，无水平溢出。

## 发现与修正历史

| 级别 | 发现 | 修正 | 复验证据 |
|---|---|---|---|
| P2，已关闭 | 初版独立预览 min-height 与 padding 叠加产生额外滚动条 | standalone 增加 border-box | 03 初版对比 04 修正版、05 全图 |
| P1，已关闭 | 真实 DSH 浅色主题卡片回退为深色，文字却使用宿主黑色 | 使用实际 `--dsw-alias-bg-layer-2` 及 bg-base 回退，控件继承 color-scheme | 08 浅色集成截图中卡片和文字对比度正常 |
| P2，已关闭 | 原生窗口旧高度不足以保证最大宠物与菜单空间 | 容器调整为 320×560 | 原生冒烟断言最大 224 尺寸宠物、气泡边界正常；06 实际窗口 |
| P3，保留 | 原版字体与本机浏览器字体栅格、字重略有不同，整体中心约数像素偏移 | 当前采用系统字体，未复制 Codex 字体资产 | 07 局部组合；不影响结构、文案和操作 |
| P3，保留 | 滑块没有原图同样的浅蓝已走轨道、预览小图大小有轻微差异 | 控件语义与大小调节正常，可后续精修 | 07 局部组合 |

一次中间截图在图集尚未全部加载时抓取，未作为最终结果；04、05、07 已用完整加载的截图替换并再次检查。

## 五项视觉检查

- 字体与排版：标题、正文、次级描述层级完整；中文字体和字重保留上述 P3 差异。
- 间距与布局：768px 设置列、9 行列表、自定义目录和外观区对应；窄屏操作区换行，长描述换行，创建弹窗按钮在屏内。
- 颜色与主题：深色接近原图；浅色跟随宿主，已修复实际对比度问题。
- 图片质量：全部为本机 Codex 原始透明 WebP，无 SVG/CSS/emoji 替代；9 只完整显示，动画截帧不同不作为画质缺陷。
- 文案与内容：保留原版名称和描述、选择/已选/刷新/收起/目录/大小；创建文案准确说明复制到 Codex 执行，不宣称后台已自动生图。

## 交互验证

- 选择 Dewey 后刷新仍已选，随后恢复 Codex；收起/恢复有效。
- 右键菜单打开、点击“跳一跳”关闭菜单并触发动作。
- Web 拖动从 (254,682) 到 (114,402)，刷新后 DOM 几何仍为 (114,402)；键盘大小由 120 调为 124，后端读取确认保存。
- 窄屏创建描述、空输入禁用、复制 Skill 指令、成功状态、可手工复制的文本框已验；最终预览无浏览器错误/警告日志。
- 原生 Electron 的独立窗口、透明、置顶、状态同步、恶意资源拒绝、最大尺寸边界和关闭清理已测试。
- 本轮测试改动的预览大小、位置与角色已恢复默认；不修改正常用户 profile。

## 验收范围与未验证事项

本报告通过的是已有设置与显示交互的设计验收，**不代表全部产品需求端到端完成**。未实际生成新宠物；未配置模型执行真实 DSH 任务链；未验证正式 Desktop 安装包和真实多显示器硬件。对应事项在当前待办中保留。

实施检查项：已修复上述 P1/P2；保留 P3 字体与滑块精修；后续完成真实 Skill 和模型任务验收。

final result: passed
