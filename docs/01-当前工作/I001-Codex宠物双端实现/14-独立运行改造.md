# 独立运行改造

更新时间：2026-09-09 14:11（Asia/Shanghai）

用户确认 DSH 使用者无需安装 Codex。本次移除 Host 对 Codex CLI 的查找与启动，内置图集纳入源码及发行包，创建 Skill 位于插件 skills/hatch-pet，按 DSH 可用工具执行。Skill 是协议兼容适配版，并非原封不动的 Codex Skill；没有复制参考仓库代码。

自定义目录使用 DSH 数据根下 pets。旧 Codex 原文件保持不变，本机未发现待复制的自定义宠物。设置显示 DSH 自定义宠物路径。创建入口使用 DSH sessions.create/open 和 session.prompt；RPC 拒绝显示错误，不伪报成功。独立预览明确禁用会话创建。

验证：类型检查、12/12 测试、构建通过；npm pack 生成安装包，检查 9 份 WebP 和 Skill，排除含本机安装路径的 source.json；隔离 Electron 启动新 Host 后实际 bundle 渲染与会话气泡测试通过。首次渲染复测因旧 14099 服务停止失败，已将测试修正为自带隔离 Host，复测通过。

包：michengai-dsh-codex-pet-0.1.0.tgz。正常 profile Junction 仍指向工作区，重启服务后加载新 Host；未擅自重启用户进程。完整真实生图、多会话全面复刻与正式 Desktop 安装验收仍未完成。
